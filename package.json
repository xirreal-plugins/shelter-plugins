{
   "devDependencies": {
      "@biomejs/biome": "2.3.11",
      "@uwu/lune": "^1.5.1",
      "@uwu/shelter-defs": "^1.5.1"
   },
   "type": "module",
   "scripts": {
      "format": "biome format --write .",
      "build": "pnpm exec lune ci && cd site && pnpm exec astro build && cp -r ./dist/* ../dist"
   }
}
